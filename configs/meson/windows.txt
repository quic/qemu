# target-specific defaults, can still be overridden on
# the command line

[built-in options]
bindir = ''
# QUIC-specific: commented-out due to QTOOL-108371
# prefix = '/qemu'

[project options]
qemu_suffix = ''
